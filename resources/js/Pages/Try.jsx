import React from "react";
import { useForm } from "@inertiajs/react";
import InputError from "@/Components/InputError";

export default function Try (){

    // const { data, setData, post, progress, errors } = useForm({
    //     name: null,
    //     avatar: null,
    // })

    // function submit(e) {
    //     e.preventDefault()
    //     post('/try')
    //   }

    // return(
    //     <div className="relative w-full h-screen bg-light-1">
    //         <div className="absolute start-1/2 top-1/2 w-1/2 h-96 transform -translate-x-1/2 -translate-y-1/2 flex flex-col bg-white rounded-3xl shadow-lg">
    //             <form onSubmit={submit} encType="multipart/form-data">
    //                 <input type="text" name='name' value={data.name} onChange={(e) => setData(e.target.name, e.target.value)}/>
    //                 <input type="file" name='avatar' onChange={(e) => setData(e.target.name, e.target.files[0])} />
    //                 <InputError message={errors.avatar} className="mt-2" />
    //                 <button className="w-fit py-2 px-5 bg-primary text-white rounded-lg">Submit</button>
    //             </form>
    //         </div>
    //     </div>
    // );

    return(
        <>
            hellow world
        </>
    )
}